import { createTheme } from '@mui/material/styles';

const theme = createTheme(
    {
        palette: {
            custom: {
                light: '#FDEEDC',
                main: '#FFD8A9',
                dark: '#F1A661',
            }
        }
    }
)

export default theme;